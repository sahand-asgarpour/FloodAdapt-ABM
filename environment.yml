name: fa_abm
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - jupyterlab
  - ipykernel
  - pip:
      - -e .[dev,pipeline]
      # Optional DYNAMO-M native framework for Phase 4b-full / PRE.1 (see pyproject 'dynamo' extra):
      # - mesa
      # - honeybees
